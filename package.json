{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "predeploy" : "npm run build",
    "deploy" : "gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^11.1.0",
    "ua-parser-js": "^2.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^6.0.5"
  }
}
